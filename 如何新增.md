# 如何新增内容

这个站点是纯静态页面，托管在 GitHub Pages，改完 `git push` 到 `main` 分支约 1 分钟自动生效。

## 目录结构

```
aurora-solutions/
├── index.html                      首页：按品牌分组的产品卡片
├── engagelab/                      品牌命名空间：EngageLab（出海）
│   └── ma/                         产品：营销自动化 MA
│       ├── index.html             MA 产品页：客户卡片列表
│       └── miniwo/
│           └── index.html         客户方案页
├── jpush/  （以后：极光 JPush 国内产品，同样套路）
└── 如何新增.md
```

层级：**首页(产品) → 产品页(客户) → 客户方案页**。品牌只体现在文件夹路径（`/engagelab/ma/...`），首页按品牌分组展示产品。

## 三种新增操作

### 1) 新增一个「方案」（最常见）

比如给 MA 再加一个方案 `acme`：

1. 用 `engagelab-ma-solution` skill 生成方案 HTML。
2. 存成 `engagelab/ma/acme/index.html`。
   - 方案页顶部"返回"链接保持 `href="../"`，会回到 MA 方案库列表页。
3. 打开 `engagelab/ma/index.html`，在 `<script>` 里的 **`SOLUTIONS` 数组**加一条即可（列表页是数据驱动渲染的，**不用手写卡片 HTML**）：

```js
{ g:'电商 · 零售 · 消费品', icon:'🛒', name:'Acme（示例）', href:'./acme/',
  desc:'一句话简介：六大旅程分别是什么',
  tags:['C 端','电商','订阅制'] },
```

字段说明：
- `g` — 分组名，必须是文件顶部 `GROUPS` 数组里已有的值；要开新分组就先往 `GROUPS` 里加一个（顺序即展示顺序）。
- `tags` — 筛选标签。想让它能被顶部筛选器选中，标签要出现在 `FILTERS` 数组里；`客户方案 / 双边平台 / 强合规` 会在卡片上高亮显示（见 `HOT_TAGS`）。
- 搜索框会同时匹配 `name` + `desc` + `tags`，无需额外配置。
- 页面顶部的方案总数、每个分组的计数都是自动算的，不用手动改。

### 2) 新增一个「产品」（EngageLab 下）

比如加 AppPush：

1. 建目录 `engagelab/apppush/`，放一个 `index.html`（复制 `engagelab/ma/index.html` 改标题、面包屑、客户卡片；"返回"仍指向 `../../`）。
2. 在首页 `index.html` 的 EngageLab `brand-group` 里加一张产品卡片，`href="./engagelab/apppush/"`。

### 3) 新增一个「品牌」（如极光 JPush 国内）

1. 建顶层目录 `jpush/`，下面按 `产品/客户/` 组织（同 EngageLab）。
2. 在首页 `index.html` 复制一个 `.brand-group` 区块（首页注释里有占位示例），品牌标签改成"极光 JPush · 国内"，加该品牌的产品卡片。

## 卡片模板

```html
<a class="card" href="./目标路径/">
    <div class="card-icon">🎯</div>
    <div class="card-content">
        <h2>标题</h2>
        <p>一句话简介</p>
    </div>
    <div class="card-meta">
        <span class="badge">标签</span>
        <span class="card-arrow">→</span>
    </div>
</a>
```

## 本地预览

直接双击任意 `index.html` 即可在浏览器打开（纯静态、无外部依赖）。卡片链接是相对路径，本地和线上都能正常跳转。

## 发布

```
git add .
git commit -m "add: <说明>"
git push
```

GitHub Pages 已开在 `main` 分支根目录，push 后自动部署到 https://zhutmac.github.io/aurora-solutions/
